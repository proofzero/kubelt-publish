"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.copyBuffer = exports.sliceString = exports.sliceBuffer = exports.copyBufferIntoBuffer = void 0;
const copyBufferIntoBuffer = (source, target, targetFrom) => {
    source.copy(target, targetFrom, 0, source.byteLength);
};
exports.copyBufferIntoBuffer = copyBufferIntoBuffer;
const sliceBuffer = (source, offset, length) => {
    return source.subarray(offset, offset + length);
};
exports.sliceBuffer = sliceBuffer;
const sliceString = (source, offset, length) => {
    return source.slice(offset, offset + length);
};
exports.sliceString = sliceString;
const copyBuffer = (source, offset, length) => {
    return Buffer.from(source.subarray(offset, offset + length));
};
exports.copyBuffer = copyBuffer;
//# sourceMappingURL=utils.js.map