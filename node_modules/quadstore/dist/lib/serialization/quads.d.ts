/// <reference types="node" />
import type { DataFactory } from 'rdf-js';
import type { Prefixes, Quad, TermName } from '../types';
export declare const quadWriter: {
    writtenValueBytes: number;
    write(prefix: string, value: Buffer, quad: Quad, termNames: TermName[], prefixes: Prefixes): string;
};
export declare const quadReader: {
    subject: null;
    predicate: null;
    object: null;
    graph: null;
    read(key: string, keyOffset: number, value: Buffer, valueOffset: number, termNames: TermName[], factory: DataFactory, prefixes: Prefixes): Quad;
};
