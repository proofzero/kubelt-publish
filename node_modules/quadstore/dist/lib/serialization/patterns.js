"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.writePattern = void 0;
const xsd = __importStar(require("./xsd"));
const fpstring_1 = require("./fpstring");
const utils_1 = require("../utils");
const terms_1 = require("./terms");
const patternLiteralWriter = {
    write(term) {
        if (term.language) {
            return terms_1.langStringLiteralWriter.write(undefined, undefined, term, utils_1.separator);
        }
        if (term.datatype) {
            switch (term.datatype.value) {
                case xsd.string:
                    return terms_1.stringLiteralWriter.write(undefined, undefined, term);
                case xsd.integer:
                case xsd.double:
                case xsd.decimal:
                case xsd.nonPositiveInteger:
                case xsd.negativeInteger:
                case xsd.long:
                case xsd.int:
                case xsd.short:
                case xsd.byte:
                case xsd.nonNegativeInteger:
                case xsd.unsignedLong:
                case xsd.unsignedInt:
                case xsd.unsignedShort:
                case xsd.unsignedByte:
                case xsd.positiveInteger:
                    return terms_1.numericLiteralWriter.write(undefined, undefined, term, utils_1.separator, fpstring_1.encode(term.value), true);
                case xsd.dateTime:
                    return terms_1.numericLiteralWriter.write(undefined, undefined, term, utils_1.separator, fpstring_1.encode(new Date(term.value).valueOf()), true);
                default:
                    return terms_1.genericLiteralWriter.write(undefined, undefined, term, utils_1.separator);
            }
        }
        return terms_1.stringLiteralWriter.write(undefined, undefined, term);
    }
};
const writePattern = (pattern, prefix, termNames, prefixes) => {
    let gt = prefix;
    let lt = prefix;
    let gte = true;
    let lte = true;
    let didRangeOrLiteral = false;
    let remaining = Object.entries(pattern).filter(([termName, term]) => term).length;
    if (remaining === 0) {
        lt += utils_1.boundary;
        return { gt, lt, gte, lte };
    }
    for (let t = 0; t < termNames.length && remaining > 0; t += 1) {
        const term = pattern[termNames[t]];
        if (!term) {
            return false;
        }
        if (didRangeOrLiteral) {
            return false;
        }
        switch (term.termType) {
            case 'Range':
                didRangeOrLiteral = true;
                if (term.gt) {
                    gt += patternLiteralWriter.write(term.gt);
                    gte = false;
                }
                else if (term.gte) {
                    gt += patternLiteralWriter.write(term.gte);
                    gte = true;
                }
                if (term.lt) {
                    lt += patternLiteralWriter.write(term.lt);
                    lte = false;
                }
                else if (term.lte) {
                    lt += patternLiteralWriter.write(term.lte);
                    lte = true;
                }
                break;
            case 'Literal':
                didRangeOrLiteral = true;
                gt += patternLiteralWriter.write(term);
                gte = true;
                lt += patternLiteralWriter.write(term);
                lte = true;
                break;
            case 'NamedNode':
                gt += terms_1.namedNodeWriter.write(undefined, undefined, term, prefixes);
                gte = true;
                lt += terms_1.namedNodeWriter.write(undefined, undefined, term, prefixes);
                lte = true;
                break;
            case 'BlankNode':
                gt += terms_1.blankNodeWriter.write(undefined, undefined, term);
                gte = true;
                lt += terms_1.blankNodeWriter.write(undefined, undefined, term);
                lte = true;
                break;
            case 'DefaultGraph':
                gt += terms_1.defaultGraphWriter.write(undefined, undefined, term);
                gte = true;
                lt += terms_1.defaultGraphWriter.write(undefined, undefined, term);
                lte = true;
                break;
        }
        remaining -= 1;
        if (remaining > 0 && t < termNames.length - 1) {
            gt += utils_1.separator;
            lt += utils_1.separator;
        }
    }
    if (lte) {
        if (didRangeOrLiteral) {
            lt += utils_1.boundary;
        }
        else {
            lt += utils_1.separator + utils_1.boundary;
        }
    }
    else {
        lt += utils_1.separator;
    }
    if (gte) {
        if (!didRangeOrLiteral) {
            gt += utils_1.separator;
        }
    }
    else {
        if (didRangeOrLiteral) {
            gt += utils_1.boundary;
        }
        else {
            gt += utils_1.separator + utils_1.boundary;
        }
    }
    return { gt, lt, gte, lte };
};
exports.writePattern = writePattern;
//# sourceMappingURL=patterns.js.map