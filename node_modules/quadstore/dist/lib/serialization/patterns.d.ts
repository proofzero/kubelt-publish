import type { Pattern, Prefixes, TermName } from '../types';
export declare const writePattern: (pattern: Pattern, prefix: string, termNames: TermName[], prefixes: Prefixes) => false | {
    gt: string;
    gte: boolean;
    lt: string;
    lte: boolean;
};
