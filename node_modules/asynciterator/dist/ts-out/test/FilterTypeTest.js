import { ArrayIterator } from '../asynciterator';
const iterator = new ArrayIterator([1, 'h', 4, 5, 7, 's']);
const filteredIteratorReturnIs = iterator.filter((item) => typeof item === 'string');
const filteredIteratorReturnBool = iterator.filter((item) => typeof item === 'string');
const filteredIteratorReturnTrue = iterator.filter(() => true);
export { filteredIteratorReturnIs, filteredIteratorReturnBool, filteredIteratorReturnTrue, };
